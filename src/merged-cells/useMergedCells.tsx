import React from 'react'
import { MergedData } from './merged-data.types'

/**
 @todo Hook that will provide necessary functions for the core
 but we just enable this if merged data is received
 **/

export function useMergedCells(data: MergedData) {}
